__pycache__/
*.pyc
/build/
/dist/
*.egg-info/
/.venv/
/.pytest_cache/
/.ruff_cache/
# ansible-galaxy collection build output
*.tar.gz
# a rendered brain, when you try the engine locally
/out/
# a Claude Code session working in a worktree of this repo (the lab does the same)
/.claude/worktrees/
