# brain

The brain laid down by the engine on its host: `home.yml` and the secret
values handed over, `check`, `render` into the root, `up`, then `apply`
(the conductor). **Lands with 0.2.** Contract: [`defaults/main.yml`](defaults/main.yml).
