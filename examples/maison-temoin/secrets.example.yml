# The witness house's secrets — FAKE values, for CI and for trying the engine.
# A real house keeps these in its own store (sops, age, a .env...) and hands
# them to the engine as values: `--secrets FILE` or REGIE_SECRET_<NAME>.
# `regie mint home.yml` writes a file like this one with fresh random values.
mqtt_password_home: example-home-password
mqtt_password_zigbee2mqtt_main: example-z2m-password
mqtt_password_kitchen_energy: example-meter-password
zigbee_main_network_key: [1, 3, 5, 7, 9, 11, 13, 15, 0, 2, 4, 6, 8, 10, 12, 13]
zigbee_main_pan_id: 6754
zigbee_main_ext_pan_id: [221, 221, 221, 221, 221, 221, 221, 221]
oidc_client_secret: example-oidc-client-secret
