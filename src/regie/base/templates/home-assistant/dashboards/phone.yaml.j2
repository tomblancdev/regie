# rendered by La Régie — the family's dashboard, built as a structure (dash.py):
# the descent, one page per rung. The house opens on one screen; a room, a group
# of lights and a place inside it are pages of their own, each with a back arrow;
# the last rung is Home Assistant's own light panel and we never redraw it.
# Nothing here is written by hand — the shape follows the rooms' files.
{{ dashboard | to_block }}
