# rendered by La Régie {{ version }} — EMPTY ON PURPOSE, and put back empty at
# every render. Automations are packages (home.yml, a pack): those show
# read-only in the UI. A draft saved here from the UI works until the next
# render, then it is gone — promote it to a package first.
[]
