# rendered by La Régie — the house's skin (home.yml `house.theme`). The palette
# says what a colour IS; the names below are the frontend's own. Home Assistant
# merges every file in this directory, so one house may hold several themes and
# `frontend.reload_themes` picks a change up without a restart.
{{ theme_file | to_block }}
