{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "no-environment: ok — the standard, not a network",
  "$id": "https://github.com/tomblancdev/regie/schema/home.schema.json",
  "title": "home.yml — a house, for La Régie (schema 1)",
  "description": "The contract every house writes to. Things are data: an id, a room, a kind, how it is reached, and since schema 1 / engine 0.4 what it is FOR (`role`). Packs add fields to a thing (their schema fragment is merged before validation); kinds, via and roles are OPEN vocabularies — an unknown value is a warning, never an error, so a new use case is a pack, not a schema bump. The vocabulary (rooms' scenes, modes, effects, scenarios) is the product's standard library: its shapes live here, `include:` brings them in from small files, the packs render them.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "house",
    "profile",
    "areas",
    "things"
  ],
  "properties": {
    "schema": {
      "const": 1,
      "description": "the schema version this file is written to; `regie migrate` moves a file forward"
    },
    "house": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "label"
      ],
      "properties": {
        "name": {
          "$ref": "#/$defs/id",
          "description": "an id for the house (paths, topics)"
        },
        "label": {
          "type": "string",
          "description": "what the family calls it"
        },
        "lang": {
          "type": "string",
          "pattern": "^[a-z]{2}$",
          "default": "en",
          "description": "the labels' language (labels/<lang>.yml)"
        },
        "timezone": {
          "type": "string",
          "default": "UTC"
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "the brain's URL for the phones (internal and external are the same door)"
        },
        "country": {
          "type": "string",
          "pattern": "^[A-Z]{2}$"
        },
        "latitude": {
          "type": "number"
        },
        "longitude": {
          "type": "number"
        },
        "elevation": {
          "type": "integer"
        },
        "unit_system": {
          "enum": [
            "metric",
            "us_customary"
          ],
          "default": "metric"
        },
        "my": {
          "type": "boolean",
          "default": true,
          "description": "My Home Assistant (my.home-assistant.io): true = Home Assistant's default, every OAuth consent redirects through the project's page; false = the brain's own door is the callback (<url>/auth/external/callback - what a vendor's app registers) and default_config is rendered explicitly without `my`"
        }
      }
    },
    "profile": {
      "$ref": "#/$defs/id",
      "description": "where the brain runs: a folder under profiles/ (ct, and later pi, vm, docker)"
    },
    "packs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/id"
      },
      "uniqueItems": true,
      "default": [],
      "description": "the use cases enabled: product packs (packs/) and the house's own (paths.house_packs)"
    },
    "paths": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "root": {
          "type": "string",
          "description": "the brain's tree on its host; the profile's default otherwise"
        },
        "house_packs": {
          "type": "string",
          "description": "a directory of the house's own packs, relative to home.yml"
        },
        "units_dir": {
          "type": "string",
          "description": "where the host's units go (`regie up`); the profile's default otherwise"
        }
      }
    },
    "owner": {
      "type": "object",
      "additionalProperties": false,
      "description": "the brain's own owner account — the break-glass login that needs no provider; its password is the secret `owner_password`. Created by `regie apply` at the first boot (Home Assistant's onboarding), never by a person at a screen.",
      "properties": {
        "username": {
          "type": "string",
          "default": "owner"
        },
        "label": {
          "type": "string",
          "default": "Owner"
        }
      }
    },
    "backup": {
      "type": "object",
      "additionalProperties": false,
      "description": "Home Assistant's own backup, scheduled by `regie apply` (encrypted with the secret `backup_password`)",
      "properties": {
        "time": {
          "type": "string",
          "pattern": "^[0-2][0-9]:[0-5][0-9]$",
          "default": "04:00",
          "description": "local time of the nightly backup"
        },
        "copies": {
          "type": "integer",
          "minimum": 1,
          "default": 7,
          "description": "how many to keep"
        }
      }
    },
    "tokens": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/id"
      },
      "uniqueItems": true,
      "default": [],
      "description": "long-lived access tokens the house's own machines need (a scraper, an automation that calls the API) — minted once by `regie apply`, kept root-only under <root>/.regie/tokens/<name>, never printed"
    },
    "floors": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/floor"
      },
      "description": "the floors the areas name (optional: a floor an area names without a line here is created with its id as its name)"
    },
    "pins": {
      "type": "object",
      "additionalProperties": false,
      "description": "the images' tags; the profile carries the versions this release was tested against, a house may override (doctor says so)",
      "properties": {
        "home_assistant": {
          "type": "string"
        },
        "mosquitto": {
          "type": "string"
        },
        "zigbee2mqtt": {
          "type": "string"
        }
      }
    },
    "proxy": {
      "type": "object",
      "additionalProperties": false,
      "description": "a reverse proxy in front of the brain (optional)",
      "properties": {
        "trusted": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "its addresses or networks (X-Forwarded-For is trusted from these)"
        },
        "via": {
          "type": "string",
          "description": "the proxy's address the brain proves its door through before promoting the HTTP config (default: the one trusted proxy when it is a single host) - on the brain the door's name may resolve to the brain itself"
        }
      }
    },
    "oidc": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "client_id",
        "discovery_url"
      ],
      "description": "an OpenID Connect door (optional — without it, Home Assistant's own login). The client secret is the secret `oidc_client_secret`.",
      "properties": {
        "client_id": {
          "type": "string"
        },
        "discovery_url": {
          "type": "string",
          "format": "uri"
        },
        "display_name": {
          "type": "string"
        },
        "roles": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "admin": {
              "type": "string",
              "description": "the provider's group that makes an admin"
            },
            "user": {
              "type": "string",
              "description": "the provider's group that lets a person in"
            }
          }
        },
        "features": {
          "type": "object",
          "description": "the component's `features:` block, passed as is (automatic_person_creation, include_groups_scope, default_redirect, ...)"
        },
        "claims": {
          "type": "object",
          "description": "the component's `claims:` block, passed as is (display_name, username, groups)"
        }
      }
    },
    "zigbee": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "coordinators"
      ],
      "properties": {
        "channel": {
          "type": "integer",
          "minimum": 11,
          "maximum": 26,
          "default": 25,
          "description": "fixed before the first pairing; changing it re-pairs the mesh"
        },
        "coordinators": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/coordinator"
          }
        }
      }
    },
    "thread": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "network_name",
        "border_routers"
      ],
      "description": "the Thread mesh: the network the house owns, and the border router(s) holding it. The DATASET (the key, the PAN ids, the mesh-local prefix) is not here and never will be - it is minted into the secrets and pushed onto the router by the fleet; the engine only introduces the router to Home Assistant, and only while it is already holding this network.",
      "properties": {
        "network_name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 16,
          "description": "the network's name (<= 16 chars by the spec) - the engine refuses to introduce a border router holding any other, because Home Assistant MINTS a network of its own on a router that holds none"
        },
        "channel": {
          "type": "integer",
          "minimum": 11,
          "maximum": 26,
          "description": "fixed before the first join, like Zigbee's; on a two-radio box it may not be the Zigbee channel"
        },
        "border_routers": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/border_router"
          }
        }
      }
    },
    "matter": {
      "type": "object",
      "additionalProperties": false,
      "description": "the Matter server's policy (pack matter)",
      "properties": {
        "only_fabric": {
          "type": "boolean",
          "default": false,
          "description": "the brain is a thing's only controller: apply removes every other fabric it finds on a node the house names (the phone's commissioning stack leaves one of its own; a vendor's app another) - said in the step, idempotent"
        }
      }
    },
    "controls": {
      "type": "object",
      "additionalProperties": false,
      "description": "the family's controls (W3b, H36): what the house exposes as simple forms and visible switches — every autonomous piece has an off-switch a person can reach",
      "properties": {
        "panel": {
          "type": "boolean",
          "default": false,
          "description": "the settings view:each room's default LOOKS become selects the family edits (per daylight, and per period with `sun` = follow the sun) — seeded once from the files, the UI owns them after; needs daylight-first defaults (H34)"
        },
        "presence": {
          "type": "boolean",
          "default": false,
          "description": "the phones drive home/away (zone.home: last one leaves → away, first one back → home) — behind input_boolean.presence_drives_mode, a visible kill-switch seeded on"
        },
        "restore_default": {
          "type": "boolean",
          "default": false,
          "description": "a light coming back from power (the wall switch, an outage) takes its room's default look instead of its last state"
        },
        "silent": {
          "type": "boolean",
          "default": true,
          "description": "the '<room> — ne répond plus' alerts (a thing unavailable an hour); off = no notification renders"
        }
      }
    },
    "mqtt": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "where the things reach the broker; the brain itself uses the loopback"
        },
        "port": {
          "type": "integer",
          "default": 1883
        }
      }
    },
    "areas": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/area"
      }
    },
    "people": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/person"
      }
    },
    "things": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/thing"
      }
    },
    "include": {
      "type": "object",
      "additionalProperties": false,
      "description": "small files merged into this one before validation — an engine feature, the same on a Pi and on a fleet. Paths and globs are relative to home.yml. rooms: one file per room, merged into the area of the same id (created when `areas:` has no line for it); modes / fx: exactly one file each, in place of the block here; scenarios: one story per file (its id = the file's stem unless the file says `id:`)",
      "properties": {
        "rooms": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ]
        },
        "modes": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ]
        },
        "fx": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ]
        },
        "scenarios": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ]
        }
      }
    },
    "modes": {
      "$ref": "#/$defs/modes"
    },
    "fx": {
      "$ref": "#/$defs/fx"
    },
    "scenarios": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/scenario"
      },
      "description": "the stories — steps, waits, effects, scenes, words; one file each under include.scenarios"
    }
  },
  "$defs": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$",
      "maxLength": 64
    },
    "ieee": {
      "type": "string",
      "pattern": "^0x[0-9a-f]{16}$"
    },
    "mac": {
      "type": "string",
      "pattern": "^([0-9a-f]{2}:){5}[0-9a-f]{2}(:[0-9a-f]{2}:[0-9a-f]{2})?$",
      "description": "a hardware address as the thing itself reports it: six bytes on Wi-Fi or Ethernet, EIGHT (an EUI-64) for a thing on Thread — what a Matter node's diagnostics hand back, and the only key most of them offer"
    },
    "floor": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "label"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "label": {
          "type": "string"
        },
        "level": {
          "type": "integer",
          "description": "0 = the ground floor; the UI sorts by it"
        }
      }
    },
    "area": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "label"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "label": {
          "type": "string"
        },
        "icon": {
          "type": "string",
          "pattern": "^mdi:[a-z0-9-]+$",
          "description": "the room's icon in Home Assistant (mdi:...), pushed by the conductor - a created area has none otherwise"
        },
        "floor": {
          "$ref": "#/$defs/id"
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "uniqueItems": true,
          "description": "what people SAY for this room (pushed to Home Assistant's area aliases beside the id; the voice pipeline matches on them)"
        },
        "roles": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "$ref": "#/$defs/role"
          },
          "description": "the roles this room has — what a thing is FOR here (lights: main · accent · lamp · strip · night · shelf · console…; the rest: remote · motion · door · screen · speaker · satellite…). An open enumeration; the walk fills them (`role:` on a thing's row)"
        },
        "scenes": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "$ref": "#/$defs/scene"
          },
          "description": "the room's looks by role; each renders as a script <room>_<scene> once a role it names is filled. `off` is implicit: every filled role off"
        },
        "defaults": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "oneOf": [
              {
                "$ref": "#/$defs/scene_ref"
              },
              {
                "$ref": "#/$defs/daylight_map"
              }
            ]
          },
          "description": "what 'on' MEANS — a LOOK, never a state (H34): daylight-first — `dark` / `dim` / `bright` keys are the base the sun drives, period keys (modes.periods) override their stretch of the day (a scene, or a partial daylight map); or period-first (the original form). sensor.<room>_default carries it; every turn-on path applies it; a default that lights nothing (`off`, or a scene whose every look is off) is refused"
        },
        "behaviours": {
          "type": "object",
          "description": "the packs' behaviour templates with this room's knobs — each pack documents its own keys (0.4: a seam, nothing reads it yet)"
        }
      }
    },
    "person": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "label"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "label": {
          "type": "string"
        },
        "admin": {
          "type": "boolean",
          "default": false
        },
        "phone": {
          "$ref": "#/$defs/id",
          "description": "the companion app's notify slug (notify.mobile_app_<phone>) — makes the person a target of the mouth (notify.<person>, notify.household)"
        }
      }
    },
    "border_router": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id"
      ],
      "description": "one Thread border router, reached over its REST API. Its address comes from the thing it names (on a two-radio box that is the SAME thing as the Zigbee coordinator - one box, one row, two radios) or from `host`.",
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "thing": {
          "$ref": "#/$defs/id",
          "description": "the thing whose `host` is the router's address"
        },
        "host": {
          "type": "string",
          "description": "the router's address, when it is not a thing"
        },
        "port": {
          "type": "integer",
          "default": 8080,
          "description": "the OpenThread REST API's port ON THE ROUTER"
        }
      }
    },
    "coordinator": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id"
      ],
      "description": "one Zigbee radio = one Zigbee2MQTT instance; reached over TCP. Its address comes from the thing it names (kind: coordinator) or from `host`.",
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "thing": {
          "$ref": "#/$defs/id",
          "description": "the thing (kind: coordinator) whose `host` is the radio's address"
        },
        "host": {
          "type": "string",
          "description": "the radio's address, when it is not a thing"
        },
        "port": {
          "type": "integer",
          "default": 6638
        },
        "adapter": {
          "type": "string",
          "default": "zstack",
          "description": "zstack (TI CC26x2), ember (Silicon Labs), deconz, zigate, zboss"
        },
        "base_topic": {
          "type": "string",
          "description": "the instance's MQTT root; `zigbee2mqtt` for the first coordinator, `zigbee2mqtt_<id>` for the others"
        }
      }
    },
    "thing": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "area",
        "kind",
        "via"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/id",
          "description": "one name, three systems: the Zigbee2MQTT friendly name, the MQTT topic, the entity's object id"
        },
        "area": {
          "$ref": "#/$defs/id",
          "description": "the room"
        },
        "kind": {
          "$ref": "#/$defs/id",
          "description": "what it is (x-known.kinds + the packs'); open"
        },
        "via": {
          "$ref": "#/$defs/id",
          "description": "how it is reached (x-known.via + the packs'); open"
        },
        "label": {
          "type": "string",
          "description": "the family's word for this one; the kind's label otherwise"
        },
        "vendor": {
          "type": "string"
        },
        "model": {
          "type": "string"
        },
        "ieee": {
          "$ref": "#/$defs/ieee",
          "description": "a Zigbee thing's address — filled by the pairing walk"
        },
        "coordinator": {
          "$ref": "#/$defs/id",
          "description": "the radio a Zigbee thing is on; the first coordinator otherwise"
        },
        "mac": {
          "$ref": "#/$defs/mac",
          "description": "a network thing's hardware address (a reservation, a row); for a Matter thing it is what its diagnostics report — six bytes on Wi-Fi, an eight-byte EUI-64 on Thread — and `apply` keys the device on it when the thing offers no serial"
        },
        "serial": {
          "type": "string",
          "minLength": 1,
          "description": "a Matter thing's serial number (its BasicInformation cluster) — the key that survives a rebuilt fabric; `pair --matter` reads it, `apply` rooms and names the device by it. Also matches a device identifier value for a thing that reports no address and no serial (a cast speaker's UUID, 0.6.2)"
        },
        "host": {
          "type": "string",
          "description": "a network thing's address"
        },
        "entity": {
          "type": "string",
          "pattern": "^[a-z_]+\\.[a-z0-9_]+$",
          "description": "the entity id when it cannot be derived from the kind and the id"
        },
        "integration": {
          "oneOf": [
            {
              "$ref": "#/$defs/id"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/id"
              },
              "minItems": 1,
              "uniqueItems": true
            }
          ],
          "description": "the Home Assistant integration(s) that reach it — one config entry each (the conductor's business); a list when one box is several things to Home Assistant (a receiver: heos + denonavr; a TV: androidtv_remote + cast)"
        },
        "bind": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/id"
          },
          "description": "a control bound inside the mesh to things or areas (an area = its Zigbee group)"
        },
        "options": {
          "type": "object",
          "description": "what a pack reads on a thing of its kinds; each pack documents its own"
        },
        "role": {
          "$ref": "#/$defs/id",
          "description": "what the thing is FOR in its room (main, accent, lamp, strip, night, shelf, console, remote, motion, door, screen, speaker, satellite…) — the key scenes and effects are written on, before the walk; open"
        },
        "at": {
          "$ref": "#/$defs/id",
          "description": "its place in the role's layout (front_left, row_3…) — needs a role; the walk assigns it"
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "uniqueItems": true,
          "description": "what people say for this one"
        }
      }
    },
    "time": {
      "type": "string",
      "pattern": "^[0-2][0-9]:[0-5][0-9]$",
      "description": "a local time, HH:MM"
    },
    "look": {
      "description": "what a role looks like in a scene: on / off, or a mapping — brightness (0-100), ct (warm | neutral | cool | kelvin), color (#rrggbb), transition (seconds). The same mapping may also name the role's PLACES (its `layout:` entries, or a prefix several of them share): those keys are looks of their own and override the base for that place. Still by role — a place name is the one `layout:` declares, never an entity id.",
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "enum": [
            "on",
            "off"
          ]
        },
        {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/place_look"
          },
          "properties": {
            "brightness": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            },
            "ct": {
              "oneOf": [
                {
                  "enum": [
                    "warm",
                    "neutral",
                    "cool"
                  ]
                },
                {
                  "type": "integer",
                  "minimum": 1000,
                  "maximum": 10000
                }
              ]
            },
            "color": {
              "type": "string",
              "pattern": "^#[0-9a-fA-F]{6}$"
            },
            "transition": {
              "type": "number",
              "minimum": 0
            }
          },
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          }
        }
      ]
    },
    "scene": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9_]*$"
      },
      "properties": {
        "label": {
          "type": "string",
          "description": "what the card calls this look (the id is what everything else uses)"
        },
        "icon": {
          "type": "string",
          "pattern": "^mdi:[a-z0-9-]+$"
        },
        "tags": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/id"
          },
          "uniqueItems": true,
          "description": "what the UI, a scenario and later the AI pick a look by (fx shapes carry the same word)"
        },
        "run": {
          "$ref": "#/$defs/sustained",
          "description": "what MOVES while this look holds — a sustained effect, stopped the moment the room takes another look"
        }
      },
      "additionalProperties": {
        "$ref": "#/$defs/look"
      },
      "description": "a persistent look for the room, BY ROLE — no entity id anywhere; a role nothing fills yet renders nothing. `label`, `icon`, `tags` and `run` are the scene's own keys and can never be role names (check refuses a role wearing one)."
    },
    "role": {
      "type": "object",
      "additionalProperties": false,
      "description": "what a room expects for one of its roles — a label, and for a ceiling of many lights its layout (the places the walk assigns with `at:`)",
      "properties": {
        "label": {
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/id"
        },
        "layout": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/id"
          },
          "uniqueItems": true,
          "description": "the places, in order (front_left, front_center, …, row_1, …): places sharing a prefix make a group of their own (light.<room>_<role>_<prefix>); effects sweep in this order"
        }
      }
    },
    "daylight_map": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "dark": {
          "$ref": "#/$defs/scene_ref"
        },
        "dim": {
          "$ref": "#/$defs/scene_ref"
        },
        "bright": {
          "$ref": "#/$defs/scene_ref"
        }
      },
      "description": "a scene per daylight (the signal sensor.daylight: dark below the horizon, dim at low sun, bright)"
    },
    "mode": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "label": {
          "type": "string"
        },
        "quiet": {
          "type": "boolean",
          "default": false,
          "description": "the mouth stays silent in this mode unless the severity is alarm (binary_sensor.house_quiet)"
        },
        "scene": {
          "description": "the scene every room takes when the house enters this mode: a scene id, `default` (the room's default for the period), `off`, or `none` — the mode touches NO light, entering it is a pure state flip (H35: `home` only ends `away`; the auto-alarm's hook)",
          "oneOf": [
            {
              "$ref": "#/$defs/id"
            },
            {
              "const": false
            }
          ]
        },
        "else": {
          "description": "the scene a room takes when it lacks the one above (off, for instance)",
          "oneOf": [
            {
              "$ref": "#/$defs/id"
            },
            {
              "const": false
            }
          ]
        },
        "rooms": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "$ref": "#/$defs/scene_ref"
          },
          "description": "per room, a scene instead of the mode's"
        },
        "away": {
          "type": "boolean",
          "default": false,
          "description": "the house is empty in this mode (binary_sensor.house_occupied reads off)"
        },
        "icon": {
          "type": "string"
        }
      }
    },
    "clock_rule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "to"
      ],
      "properties": {
        "to": {
          "$ref": "#/$defs/id",
          "description": "the mode the house enters when this period begins"
        },
        "from": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/id"
          },
          "minItems": 1,
          "description": "only when the house is in one of these modes (a cinema at 22:30 is not interrupted); any mode when absent"
        }
      }
    },
    "modes": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "modes"
      ],
      "description": "the house's state machine: input_select.house_mode, one automation per transition, the periods (four times editable in the UI, seeded from here once), the clock rules and the daylight thresholds",
      "properties": {
        "initial": {
          "$ref": "#/$defs/id",
          "default": "home",
          "description": "the mode the brain starts in the very first time (seeded once by the conductor; the UI's choice is kept after)"
        },
        "modes": {
          "type": "object",
          "minProperties": 1,
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "$ref": "#/$defs/mode"
          }
        },
        "periods": {
          "type": "object",
          "minProperties": 1,
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "$ref": "#/$defs/period"
          },
          "description": "in order of the day — each begins at its time (an input_datetime the family edits in the UI; this file only seeds it); before the first, the last one holds (night wraps)"
        },
        "clock": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "$ref": "#/$defs/clock_rule"
          },
          "description": "per period: the mode its beginning moves the house to"
        },
        "daylight": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "dark_below": {
              "type": "number",
              "default": -6,
              "description": "the sun's elevation (degrees) under which it is dark (civil twilight = -6)"
            },
            "bright_above": {
              "type": "number",
              "default": 10,
              "description": "above which it is bright"
            }
          }
        },
        "follow": {
          "type": "boolean",
          "default": true,
          "description": "when the period or the daylight changes and the house is in a mode whose scene is `default`, the rooms that are lit take their new default"
        }
      }
    },
    "shape": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "steps"
      ],
      "description": "an effect's SHAPE — what, never how: steps of level / colour / hold / transition, repeats, bricks composed (`use:`); a backend compiles it at the protocol's resolution",
      "properties": {
        "summary": {
          "type": "string"
        },
        "icon": {
          "type": "string"
        },
        "tags": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "needs": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "step": {
              "type": "number",
              "exclusiveMinimum": 0,
              "description": "the finest hold it asks for, seconds"
            }
          }
        },
        "fields": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "type": [
              "number",
              "string",
              "boolean",
              "null"
            ]
          },
          "description": "the shape's knobs with their defaults — the script's fields; a step refers to one as $name"
        },
        "steps": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "use": {
                "$ref": "#/$defs/id"
              },
              "level": {},
              "colour": {},
              "hold": {},
              "transition": {}
            }
          }
        },
        "repeat": {
          "description": "how many times the steps run ($times, or a number)"
        },
        "restore": {
          "type": "boolean",
          "default": true,
          "description": "snapshot before, put back after"
        }
      }
    },
    "fx": {
      "type": "object",
      "additionalProperties": false,
      "description": "which effects the house enables, on which backend, its own shapes",
      "properties": {
        "backend": {
          "$ref": "#/$defs/id",
          "default": "ha",
          "description": "the compiler every target goes through at 0.4 (ha: the generic light-service loop); per-protocol backends land with their bench"
        },
        "enable": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/id"
          },
          "uniqueItems": true,
          "description": "the shapes rendered as scripts; every shape when absent"
        },
        "shapes": {
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "$ref": "#/$defs/shape"
          },
          "description": "the house's own shapes, beside the product's"
        },
        "kelvin": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "warm": {
              "type": "integer"
            },
            "neutral": {
              "type": "integer"
            },
            "cool": {
              "type": "integer"
            }
          },
          "description": "what warm / neutral / cool mean in a scene (2700 / 4000 / 5500 by default)"
        }
      }
    },
    "scenario_step": {
      "description": "one step of a story: a mode, a scene (room/scene), an effect, a wait, words",
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "mode"
          ],
          "properties": {
            "mode": {
              "$ref": "#/$defs/id"
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "scene"
          ],
          "properties": {
            "scene": {
              "type": "string",
              "pattern": "^[a-z][a-z0-9_]*/[a-z][a-z0-9_]*$"
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "fx"
          ],
          "properties": {
            "fx": {
              "type": "object",
              "required": [
                "shape",
                "target"
              ],
              "properties": {
                "shape": {
                  "$ref": "#/$defs/id"
                },
                "target": {}
              },
              "additionalProperties": true
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "wait"
          ],
          "properties": {
            "wait": {
              "oneOf": [
                {
                  "type": "number",
                  "minimum": 0
                },
                {
                  "type": "string",
                  "pattern": "^[0-9]{2}:[0-9]{2}:[0-9]{2}$"
                }
              ]
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "tell"
          ],
          "properties": {
            "tell": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "message"
              ],
              "properties": {
                "message": {
                  "type": "string"
                },
                "title": {
                  "type": "string"
                },
                "severity": {
                  "enum": [
                    "info",
                    "warn",
                    "alarm"
                  ]
                }
              }
            }
          }
        }
      ]
    },
    "scenario": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "steps"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "label": {
          "type": "string"
        },
        "summary": {
          "type": "string"
        },
        "icon": {
          "type": "string"
        },
        "steps": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/scenario_step"
          }
        }
      }
    },
    "period": {
      "description": "when a period begins: a time (HH:MM), or {at, label} — the label is what the family sees on the time helper",
      "oneOf": [
        {
          "$ref": "#/$defs/time"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "at"
          ],
          "properties": {
            "at": {
              "$ref": "#/$defs/time"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "scene_ref": {
      "description": "a scene id — `off` (YAML reads a bare off as false: both are taken) or `default`",
      "oneOf": [
        {
          "$ref": "#/$defs/id"
        },
        {
          "const": false
        }
      ]
    },
    "place_look": {
      "description": "a look for ONE place of a role (or for the places sharing a prefix): on / off, or the same mapping of brightness / ct / color / transition",
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "enum": [
            "on",
            "off"
          ]
        },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "brightness": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            },
            "ct": {
              "oneOf": [
                {
                  "enum": [
                    "warm",
                    "neutral",
                    "cool"
                  ]
                },
                {
                  "type": "integer",
                  "minimum": 1000,
                  "maximum": 10000
                }
              ]
            },
            "color": {
              "type": "string",
              "pattern": "^#[0-9a-fA-F]{6}$"
            },
            "transition": {
              "type": "number",
              "minimum": 0
            }
          }
        }
      ]
    },
    "sustained": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": false,
      "description": "sustained effects, by shape. Unlike an fx (transient: snapshot -> run -> restore) a sustained effect runs for as long as the look holds, and its kill-switch is a visible helper.",
      "properties": {
        "drift": {
          "$ref": "#/$defs/drift"
        }
      }
    },
    "drift": {
      "type": "object",
      "additionalProperties": false,
      "description": "a slow colour walk: every place wanders the band on its own clock, for ever, and no two are ever in step. Brightness is NEVER touched (the scene sets it once) — a level command interrupts a colour ramp in the bulb's own firmware.",
      "required": [
        "role"
      ],
      "properties": {
        "role": {
          "$ref": "#/$defs/id",
          "description": "the role whose places drift"
        },
        "places": {
          "oneOf": [
            {
              "$ref": "#/$defs/id"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/id"
              }
            }
          ],
          "description": "which places: a prefix (row), a list, or absent for all of them"
        },
        "band": {
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "number",
            "minimum": 0,
            "maximum": 360
          },
          "description": "the hue band in degrees, walked along the colour circle so the path never washes through white (default 190-330: cyan to hot pink)"
        },
        "saturation": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100
        },
        "period": {
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "number",
            "minimum": 10
          },
          "description": "seconds for one there-and-back, spread across the places: no two share a period, so the ceiling never falls into step"
        },
        "step": {
          "type": "number",
          "minimum": 0.5,
          "description": "seconds between colour commands per place. The backend's colour floor, MEASURED: below ~2 s a new command aborts the ramp still running in the bulb and the drift reads as jitter (default 2.5)"
        }
      }
    }
  },
  "x-known": {
    "kinds": [
      "light",
      "plug",
      "switch",
      "sensor",
      "remote",
      "cover",
      "door",
      "motion",
      "cast",
      "tv",
      "speaker",
      "satellite",
      "camera",
      "lock",
      "thermostat",
      "valve",
      "printer",
      "coordinator",
      "proxy"
    ],
    "via": [
      "zigbee",
      "wifi",
      "lan",
      "mqtt",
      "ble",
      "matter",
      "thread",
      "esphome"
    ]
  }
}
